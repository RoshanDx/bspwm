#!/usr/bin/env bash

zscroll -b "⮕ " -l 30 \
        --delay 0.45 \
        --match-command "$HOME/.local/bin/zscroll-quote/get_news_status" \
        --update-check true "$HOME/.local/bin/zscroll-quote/get_news_status" &

wait

